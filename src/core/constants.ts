/** Default quantize value for MIDI editors (quarter note) */
export const DEFAULT_QUANTIZE = 0.25;

/** Tolerance for matching events at the same beat position (tight) */
export const TIME_TOLERANCE = 0.01;

/** Tolerance for chord/harmony detection (looser, catches slight timing variations) */
export const HARMONY_TOLERANCE = 0.1;
